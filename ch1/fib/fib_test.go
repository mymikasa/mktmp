package fib

import (
	"fmt"
	"testing"
)

func TestFibList(t *testing.T) {
	a := 1
	b := 2


























































































































































































































	

	for i := 0; i < 5; i++ {
		fmt.Print(" ", b)

		tmp := a
		a = b
		b = tmp + a
	}
	t.Log("Testing!!!")
	fmt.Println()
}
